# jewellify-frontend/netlify.toml

[build]
  # Build command
  command = "npm run build"
  
  # Publish directory (must match angular.json output path)
  publish = "dist/jewellify/browser"

# Redirects for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200